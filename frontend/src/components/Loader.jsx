// Simple full-width spinner used while data is being fetched.
const Loader = () => (
  <div className="loader-wrap">
    <div className="loader-stamp" role="status" aria-label="Loading" />
  </div>
);

export default Loader;
